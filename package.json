{
  "name": "cypresse2edemo",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "scripts": "node_modules/.bin/cypress run",
    "clean-reports": "rm -R -f CypressDemo/mochawesome-report && rm -R -f CypressDemo/mochawesome.json", 
    "pretest": "npm run clean-reports",
    "posttest": "npx mochawesome-merge mochawesome-report/*.json > mochawesome.json && npx marge mochawesome.json ",
    "test": "npm run scripts || npm run posttest"

  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@percy/cli": "^1.0.8",
    "@percy/cypress": "^3.1.1",
    "cypress": "^9.5.4",
    "cypress-audit": "^1.1.0",
    "cypress-mochawesome-reporter": "^3.0.1",
    "cypress-multi-reporters": "^1.6.0",
    "mocha": "^10.0.0",
    "mochawesome": "^7.1.3",
    "mochawesome-merge": "^4.2.1",
    "mochawesome-report-generator": "^6.2.0"
  },
  "dependencies": {
    "puppeteer": "^13.5.2"
  }
}
